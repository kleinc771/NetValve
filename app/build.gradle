plugins {
    id 'com.android.application'
    id 'kotlin-android'
}

android {
    namespace 'com.netvalve.app'
    compileSdk 33

    defaultConfig {
        applicationId "com.netvalve.app"
        minSdk 24
        targetSdk 33
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
