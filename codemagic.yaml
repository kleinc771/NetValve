workflows:
  android-workflow:
    name: NetValve Android Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    scripts:
      - name: Build Android APK
        script: |
          gradle assembleDebug
    artifacts:
      - app/build/outputs/apk/**/*.apk
    publishing:
      email:
        recipients:
          - kleinc771@gmail.com
